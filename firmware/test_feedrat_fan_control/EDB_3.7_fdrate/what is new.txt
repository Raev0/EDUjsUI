新功能

·机器安装调试不方便？导致装机困难，不稳定？没事，软件来支持

	·支持测试专用触摸屏程序。测试固件和批产固件all in one。留足数控代码和通信相应接口。整理接口说明。

·断点续打失败机器走G28？没事，软件来支持。

	·对应断点续打存储失败的打印机程序和提示

·通信干扰导致提示界面错乱，卡在99%后手足无措？没事，软件来支持

	·通信方式更新，新的通信会发送两次，并且会在优化过程中缩短通信字符长度。

·舵机动作太频繁？没关系，软件来支持

	·修改智能调平舵机动作顺序；新的顺序应该是平台先下降，再抬起探针。在智能调平的时候，让探针下降一次，测完九个点之后再抬起来

2.bugfix

最大的bugfix在于修改了断电续打固件相应代码：

·Issue14 ：将断电续打功能在固件中的相应代码，由过去的多个loop循环执行完，改写成一个Loop循环执行完。规避了断电续打数据在多Loop中被违法改写的风险。

其他修改集中在tft_update()上。

·Issue0 ：添加降温接口，配合触摸屏程序；如果是在打印过程中续料按取消，喷头不会降温。但是如果是在其他位置按取消，就会降温。这么设计的理由是，确保打印过程中，系统不会无缘无故丢失喷头温度。

·Issue9 ：停止打印应该补偿续打的z坐标数据，避免出现断点续打按暂停，平台超出最大范围的问题。

·Issue6 ：在打印完成后，系统进入重复打印状态。修正在这个状态下，预热和冷却的温度会停止更新，死在200度显示上。

·Issue8 ：解决停止打印后，出现SD卡无法更新的情况

                                                                    
3.代码优化

·通信优化，缩短通信数据量，减小负荷，消灭延时。整理通信协议。

·优化tft_update()，消灭不期望的异常状态位。整理控制流。







 